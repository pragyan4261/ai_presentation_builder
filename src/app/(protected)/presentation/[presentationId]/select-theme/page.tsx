import React from 'react'
import ThemePreview from './_components/ThemePreview'

type Props = {}

const page = (props: Props) => {
  return <ThemePreview height={0} width={0} />
}

export default page